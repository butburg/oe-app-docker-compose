name: Deploy Laravel Project

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.LIMA_SSH_PRIVATE_KEY }}

    - name: Add SSH key to known_hosts
      run: |
        mkdir -p ~/.ssh
        echo "$SFTP_HOST ssh-rsa c6:e6:76:98:23:31:2c:b7:ca:1a:15:47:96:91:86:54" >> ~/.ssh/known_hosts
        cat ~/.ssh/known_hosts

    - name: Upload files via SSH
      env:
        SFTP_HOST: ${{ secrets.LIMA_SFTP_HOST }}
        SFTP_USERNAME: ${{ secrets.LIMA_SFTP_USERNAME }}
      run: |
        rsync -r --delete-after --quiet $GITHUB_WORKSPACE/src/ $SFTP_USERNAME@$SFTP_HOST:oe-app/

    - name: Upload .env file
      env:
        SFTP_HOST: ${{ secrets.LIMA_SFTP_HOST }}
        SFTP_USERNAME: ${{ secrets.LIMA_SFTP_USERNAME }}
        ENV_VARIABLES: ${{ secrets.LIMA_ENV_VARIABLES }}
      run: |
        echo "$ENV_VARIABLES" | ssh $SFTP_USERNAME@$SFTP_HOST 'cat > oe-app/.env'

    - name: Run remote SSH commands
      env:
        SFTP_HOST: ${{ secrets.LIMA_SFTP_HOST }}
        SFTP_USERNAME: ${{ secrets.LIMA_SFTP_USERNAME }}
      run: |
        ssh $SFTP_USERNAME@$SFTP_HOST << 'EOF'
        cd oe-app
        composer install --no-interaction --prefer-dist --optimize-autoloader
        npm run build
        php artisan migrate --force
        php artisan config:cache
        EOF
